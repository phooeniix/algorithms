#include <iostream>
using namespace std;
void horaMinutos(int *hora, int *min){
	mnts = horas*60 + m;
	cout<<mnts<<endl;
}
int main(){
	int hora, minuto;
	cin>>hora>>minuto;
	if(hora>0)
		horaMinutos(hora, minuto);
	return 0;
}
